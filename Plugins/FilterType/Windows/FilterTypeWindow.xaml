<controls:FrostyDockableWindow x:Class="FilterType.Windows.FilterTypeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:controls="clr-namespace:Frosty.Controls;assembly=FrostyControls"
        mc:Ignorable="d"
        Title="Filter Assets" Height="500" Width="400"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FilterType;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource ListBackground}">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Filter by type:" Foreground="{StaticResource FontColor}" Margin="0,0,0,6"/>
            <ComboBox x:Name="TypeBox" Grid.Row="1" Height="26" Margin="0,0,0,12" SelectionChanged="FilterChanged"/>
            <CheckBox x:Name="ShowOnlyModifiedBox" Grid.Row="2" Content="Show only modified" Foreground="{StaticResource FontColor}" Margin="0,0,0,12" Checked="FilterChanged" Unchecked="FilterChanged"/>
            <TextBlock x:Name="ResultCountText" Grid.Row="3" Text="0 assets found" Foreground="{StaticResource FontColor}" Opacity="0.7" Margin="0,0,0,6"/>
            <ListBox x:Name="ResultsList" Grid.Row="4" DisplayMemberPath="Name" SelectionChanged="ResultsList_OnSelectionChanged" MouseDoubleClick="ResultsList_OnMouseDoubleClick"/>
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button x:Name="OpenButton" Content="Open" Width="100" Height="28" Margin="0,0,8,0" Click="OpenButton_OnClick" IsEnabled="False"/>
                <Button Content="Close" Width="90" Height="28" Click="CloseButton_OnClick"/>
            </StackPanel>
        </Grid>
    </Grid>
</controls:FrostyDockableWindow>